# src_frontend/nginx.conf (Massively UI కోసం సరిదిద్దబడినది)

server {
    listen 80;
    server_name localhost;

    root /usr/share/nginx/html; 
    index index.html index.htm;

    
    location / {
        try_files $uri $uri/ =404;
    }
    
    
}